import { colors } from "./colors";

const commonTextStyles = `
letter-spacing:0.2px
line-height:1.25;
`;

const itemMarginRight = `40px`;

export const Allstyles = {
  colors,
  commonTextStyles,
  itemMarginRight,
};
